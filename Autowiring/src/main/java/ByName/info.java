package ByName;

public class info {
	
	/*
	 In this case, spring framework attempts to find out a bean in the
	  configuration file, whose bean id (policy) is matching with the 
	  property name (categories.java private Policy policy) to be wired.
	  
	    If a bean found with class as property name then that class object 
	    will be injected into that property by calling setter injection. 
	    
	     
      If no class found then that property remains un-wired, but never 
      throws any exception just like before.

	 */

}
